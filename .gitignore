# Local test stubs. To render a page without the live API I drop a static file at
# the endpoint's own path (api/report, api/openitems, ...). Those have been swept
# into commits three times, and one of them sits where a real function serves —
# a static file there can shadow the endpoint. Only .js belongs in api/.
api/*
!api/*.js

# Punch-list snapshot served to a local browser for UI testing.
_fixture.json
